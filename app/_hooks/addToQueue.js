// const [status, setStatus] = useState(null);

// const addToQueue = async (songUri) => {
//   try {
//     const response = await axios.post("/api/spotify", { songUri });
//     setStatus("Song added to the queue!");
//   } catch (error) {
//     setStatus("Failed to add song to queue.");
//   }
// };

// return (
//   <div>
//     <button onClick={() => addToQueue(songUri)}>Add to Queaue</button>
//     {status && <p>{status}</p>}
//   </div>
// );
